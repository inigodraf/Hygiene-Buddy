<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fillViewport="true"
    android:background="@color/white"
    android:padding="24dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">

        <!-- Header Section -->
        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@string/child_profile_title"
            android:textSize="28sp"
            android:textStyle="bold"
            android:textColor="@color/text_header"
            android:gravity="center"
            android:layout_marginBottom="16dp" />

        <!-- Instruction Text -->
        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@string/child_profile_instruction"
            android:textSize="16sp"
            android:textColor="@color/subtitle_text"
            android:gravity="center"
            android:lineSpacingExtra="4sp"
            android:layout_marginBottom="40dp" />

        <!-- Avatar Selection Section -->
        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@string/choose_avatar"
            android:textSize="16sp"
            android:textStyle="bold"
            android:textColor="@color/feature_text"
            android:layout_marginBottom="8dp" />

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@string/avatar_description"
            android:textSize="14sp"
            android:textColor="@color/subtitle_text"
            android:layout_marginBottom="16dp" />

        <!-- Avatar Image with Edit Icon -->
        <RelativeLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:layout_marginBottom="32dp">

            <!-- Avatar Circle -->
            <LinearLayout
                android:id="@+id/avatarContainer"
                android:layout_width="120dp"
                android:layout_height="120dp"
                android:background="@drawable/avatar_bg"
                android:gravity="center">

                <ImageView
                    android:id="@+id/ivChildAvatar"
                    android:layout_width="80dp"
                    android:layout_height="80dp"
                    android:src="@drawable/default_avatar"
                    android:scaleType="fitCenter"
                    android:contentDescription="@string/child_avatar_description"
                    />

            </LinearLayout>

            <!-- Edit Icon Overlay -->
            <ImageButton
                android:id="@+id/btnEditAvatar"
                android:layout_width="36dp"
                android:layout_height="36dp"
                android:src="@drawable/ic_edit"
                android:background="@drawable/editbtn_bg"
                android:scaleType="centerInside"
                android:layout_alignBottom="@id/avatarContainer"
                android:layout_alignEnd="@id/avatarContainer"
                android:layout_marginEnd="8dp"
                android:layout_marginBottom="8dp"
                android:contentDescription="@string/editbtn"
                />

        </RelativeLayout>

        <!-- Child's Name Input -->
        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@string/child_name_label"
            android:textSize="16sp"
            android:textStyle="bold"
            android:textColor="@color/feature_text"
            android:layout_marginBottom="8dp" />

        <com.google.android.material.textfield.TextInputLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginBottom="24dp"
            style="@style/Widget.Material3.TextInputLayout.OutlinedBox">

            <com.google.android.material.textfield.TextInputEditText
                android:id="@+id/etChildName"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:hint="@string/child_name_hint"
                android:textSize="16sp"
                android:padding="16dp" />

        </com.google.android.material.textfield.TextInputLayout>

        <!-- Age Selection -->
        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@string/age_label"
            android:textSize="16sp"
            android:textStyle="bold"
            android:textColor="@color/feature_text"
            android:layout_marginBottom="8dp" />

        <com.google.android.material.textfield.TextInputLayout
            android:id="@+id/ageDropdownLayout"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="@string/age_label"
            style="@style/Widget.MaterialComponents.TextInputLayout.OutlinedBox.ExposedDropdownMenu">

            <AutoCompleteTextView
                android:id="@+id/actvAge"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:inputType="none"
                android:focusable="false"
                android:hint="@string/age_hint" />
        </com.google.android.material.textfield.TextInputLayout>

        <!-- Condition Tags Section -->
        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@string/condition_tags_label"
            android:textSize="16sp"
            android:textStyle="bold"
            android:textColor="@color/feature_text"
            android:layout_marginBottom="16dp" />

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:layout_marginBottom="32dp">

            <!-- ASD Checkbox -->
            <com.google.android.material.checkbox.MaterialCheckBox
                android:id="@+id/cbASD"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="@string/asd_label"
                android:textSize="16sp"
                android:textColor="@color/feature_text"
                android:paddingVertical="12dp" />

            <!-- ADHD Checkbox -->
            <com.google.android.material.checkbox.MaterialCheckBox
                android:id="@+id/cbADHD"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="@string/adhd_label"
                android:textSize="16sp"
                android:textColor="@color/feature_text"
                android:paddingVertical="12dp" />

            <!-- Down Syndrome Checkbox -->
            <com.google.android.material.checkbox.MaterialCheckBox
                android:id="@+id/cbDownSyndrome"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="@string/down_syndrome_label"
                android:textSize="16sp"
                android:textColor="@color/feature_text"
                android:paddingVertical="12dp" />

        </LinearLayout>

        <!-- Edit Note -->
        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@string/edit_note"
            android:textSize="14sp"
            android:textColor="@color/subtitle_text"
            android:gravity="center"
            android:lineSpacingExtra="2sp"
            android:layout_marginBottom="32dp" />

        <!-- Save & Continue Button -->
        <Button
            android:id="@+id/btnSaveContinue"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@string/save_continue_button"
            android:textSize="18sp"
            android:backgroundTint="#A2D1BF"
            android:textColor="#121717"
            android:paddingTop="16dp"
            android:paddingBottom="16dp" />

    </LinearLayout>

</ScrollView>