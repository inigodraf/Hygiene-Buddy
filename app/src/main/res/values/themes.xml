<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base theme -->
    <style name="Base.Theme.HygieneBuddy" parent="Theme.Material3.DayNight.NoActionBar">

        <!-- Apply global font -->
        <item name="android:fontFamily">@font/plus_jakarta_sans</item>
    </style>

    <!-- App theme -->
    <style name="Theme.HygieneBuddy" parent="Base.Theme.HygieneBuddy" />

</resources>
